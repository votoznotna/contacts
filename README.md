For up and running, have node.js install locally, then navigate to root folder of the project and proceed with the following:
1. run "npm install".
2. run "npm start".
4. navigate to http://localhost:4200 on any browser.
